// Moved into @liveleagues/core so the Expo mobile app shares the exact same
// live-data hooks. This shim keeps existing web imports (`@/hooks/useLive`)
// working unchanged.
export * from "@liveleagues/core/hooks/useLive";
